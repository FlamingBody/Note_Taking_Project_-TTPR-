<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= note.title %></title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="top_Container-note">
    <a href="#" onclick="document.getElementById('noteForm').submit();">← Back to all notes</a>
    <h1>Viewing Note</h1>
  </div>

  <div class="main_container">
    <form id="noteForm" action="/update/<%= note.id %>" method="POST" class="note_view">
      <input type="text" name="title" value="<%= note.title %>" required />
      <textarea name="content" required><%= note.content %></textarea>
    </form>
  </div>
</body>
</html>
